# Ukol1Advanced

Static HTML page for a school web programming assignment.

It uses a table-based layout, navigation links, semantic HTML elements, contact information, and an image map (`mapa.jpg`) with clickable university building links.

Open `index.html` in a browser to view the project.
